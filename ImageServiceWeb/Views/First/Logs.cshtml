@model ImageServiceWeb.Models.LogModel
@{
    ViewBag.Title = "Logs";
}

<h2>Logs</h2>

<table class="table">

    @foreach (var item in Model.Logs)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item)
            </td>
        </tr>
    }
</table>